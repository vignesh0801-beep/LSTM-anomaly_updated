install everything in requirements either using venv or pip
run hands_data_generation.py/pose_data_generation.y
change labels for neutral and violence datasets and train the model
run train_model.py to generate h5 file and train model via LSTM
run pose_LSTM_realtime
open the local host url for videofeed and statistics
message should be sent via twilio api for evry violent action if trained properly
